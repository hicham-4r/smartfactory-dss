# Phase 7 Runtime Evidence

Run the Step 22G `scripts/verify.ps1` script. After both complete regression suites pass, it creates `FINAL_ACCEPTANCE_RUNTIME_EVIDENCE.txt` in this directory.

The generated file intentionally excludes secrets, prompts, model output, session contents, database records, and operational data.
